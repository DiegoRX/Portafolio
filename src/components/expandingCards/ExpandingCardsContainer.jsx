import React from 'react';

const ExpandingCardsContainer = ({children}) => {
    return(
        <div className="container">
            {children}
        </div>
    )
}

export default ExpandingCardsContainer;